import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.businesscalls.app',
  appName: 'BusinessCalls Technician',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
